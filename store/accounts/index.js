const state = () => ({
  accounts: [],
  banks: [],
  //   accounts: process.client ? localStorage.getItem("accounts") : [],
  //   banks: process.client ? localStorage.getItem("banks") : [],
});

export { state };
